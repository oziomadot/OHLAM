import { Audio } from 'expo-av';

class SoundManager {
  static async playSpinSound() {
    try {
      // Placeholder for spin sound - can be implemented later
      console.log('Playing spin sound');
    } catch (error) {
      console.log('Sound error:', error);
    }
  }

  static async playCoinSound() {
    try {
      // Placeholder for coin sound - can be implemented later
      console.log('Playing coin sound');
    } catch (error) {
      console.log('Sound error:', error);
    }
  }
}

export default SoundManager;
